---
description: "Close a spike via the disposition gate — `--promote` files a [Feature] follow-up, `--discard` writes a memo."
---

Follow the apexyard skill defined in @.claude/skills/spike-close/SKILL.md and execute its process step by step.

$ARGUMENTS
