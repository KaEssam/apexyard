---
description: "Declare an active ticket so the ticket-first hook lets code edits through. Accepts `<N>` or `<owner>/<repo>#<N>`."
---

Follow the apexyard skill defined in @.claude/skills/start-ticket/SKILL.md and execute its process step by step.

$ARGUMENTS
